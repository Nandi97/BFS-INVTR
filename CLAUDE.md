@AGENTS.md

# BFS Inventory — Claude Code Context

## What this is

A Next.js 16 web app replacing the Excel-based inventory workflow for **Beauty First / Beauty Logix** warehouse (`order@beautylogix.ca`). The goal is stock-level visibility and automated low-stock notifications — transactions still happen in QuickBooks.

**Production URL**: `https://bfs.kigtech.digital`
**Hosting**: Vercel (Pro — cron jobs enabled)
**Database**: Neon PostgreSQL (shared between local dev and prod — same `DATABASE_URL`)

---

## Tech Stack

| Layer            | Choice                                                                |
| ---------------- | --------------------------------------------------------------------- |
| Framework        | Next.js 16.2.9, App Router, React 19, TypeScript                      |
| Database         | PostgreSQL via Prisma 7 (Neon)                                        |
| Auth             | better-auth 1.6                                                       |
| UI               | shadcn (radix-nova style) + Tailwind 4                                |
| Data fetching    | TanStack Query v5 + axios                                             |
| Forms            | react-hook-form + Zod 4                                               |
| Charts           | recharts                                                              |
| Email            | nodemailer (Gmail app password)                                       |
| Font             | Plus Jakarta Sans → `--font-sans` (variable set directly on `<body>`) |
| Excel generation | ExcelJS v4 (email attachments + reports)                              |

---

## Critical Prisma 7 Rules

- **No `url` in `datasource` block** — connection config lives in `prisma.config.ts`
- **Client generates to** `../generated/prisma/client.ts`
- **Adapter required** — uses `PrismaPg` with `pg.Pool`
- **Import path**: `import { prisma } from "@/lib/prisma"`
- **After schema changes**: run `npx prisma migrate dev --name <name>` then `npx prisma generate` then **restart the dev server** — the running server caches the old compiled Prisma client and won't see new fields until restart

## Critical Zod 4 Rules

- Use `z.number()` not `z.coerce.number()` (coerce returns `ZodPipe`, breaks resolver)
- Use `valueAsNumber` on number inputs: `onChange={(e) => field.onChange(e.target.valueAsNumber)}`
- `invalid_type_error` option removed — use `error` or omit

## radix-ui Bundled Package

All Radix imports come from the single bundled `radix-ui` package, **not** individual `@radix-ui/*` packages:

```ts
import { Label as LabelPrimitive, Slot } from "radix-ui";
// Use .Root sub-components:
<LabelPrimitive.Root />
<Slot.Root />
```

---

## Layout Structure

```
app/(dashboard)/layout.tsx          ← reads sidebar:state cookie, SidebarProvider
  AppSidebar                        ← collapsible="icon", cookie persisted
  SidebarInset
    AppHeader                       ← breadcrumbs + theme toggle + user nav
    <div className="flex flex-1 flex-col gap-6 p-6 overflow-y-auto">
      {children}                    ← all pages get p-6 gap-6 automatically
```

Each page uses `<div className="space-y-6">` — no extra padding wrapper needed.

---

## Component Structure Convention

All page routes under `app/(dashboard)` and `app/(auth)` are **thin entry points** (5–15 lines: route params, metadata, one component render). All UI logic lives in `components/<module>/`:

```
components/<module>/
  dashboard/   ← list/overview page component (e.g. products-dashboard.tsx)
  create/      ← shared add-or-edit form (e.g. product-form.tsx)
  view/        ← detail page component (e.g. product-view.tsx, po-detail-sheet.tsx)
```

Not every module uses all three subdirs — only what the feature requires.

Examples:

- `components/products/dashboard/products-dashboard.tsx` → rendered by `app/(dashboard)/products/page.tsx`
- `components/products/create/product-form.tsx` → shared add/edit form
- `components/products/view/product-view.tsx` → rendered by `app/(dashboard)/products/[id]/page.tsx`
- `components/zenoti/dashboard/zenoti-orders-table.tsx` → rendered by `app/(dashboard)/zenoti/page.tsx`
- `components/zenoti/view/fulfillment-view.tsx` → rendered by `app/(dashboard)/zenoti/[id]/page.tsx`
- `components/auth/login-view.tsx` → rendered by `app/(auth)/login/page.tsx`

**Do not add logic back into `page.tsx` files** — they stay thin wrappers.

---

## Sheet Form UI Convention

All `<Sheet>` slide-over forms follow this pattern:

```tsx
<SheetContent className="sm:max-w-{size} w-full overflow-y-auto">
	<SheetHeader className="px-6 pt-6 pb-2">
		<SheetTitle>…</SheetTitle>
		<SheetDescription>…</SheetDescription>
	</SheetHeader>

	<form className="space-y-5 px-6 pb-6">
		{/* fields */}
		<div className="flex justify-end gap-3 border-t pt-5">
			<Button variant="outline">Cancel</Button>
			<Button type="submit">Save</Button>
		</div>
	</form>
</SheetContent>
```

**Width guide:**

- `sm:max-w-lg` — simple forms with 3–4 fields (Set Thresholds, Add Location)
- `sm:max-w-xl` — standard forms with 5+ fields (Alert Rule, Product, Adjust Stock, Supplier)
- `sm:max-w-2xl` — complex / table-heavy sheets (New PO, PO Detail, Receive PO)

---

## Database Seed & Setup

```bash
npx prisma db seed              # seeds BF Warehouse location
npx prisma migrate deploy       # applies all migrations
npx tsx --env-file=.env scripts/seed-lead-times.ts   # brand lead times + disable direct-supply locations
npx tsx --env-file=.env scripts/set-local-target-months.ts  # sets local brands to 2-month stock target
```

## Warehouse Data Import

Product catalog (919 products) and opening stock (640 records) were imported from the Excel spreadsheet. QB is now the live source of truth — the scripts below were one-time bootstrap only.

```bash
python3 -m venv /tmp/bfs-venv
/tmp/bfs-venv/bin/pip install requests openpyxl xlrd
/tmp/bfs-venv/bin/python3 scripts/import_warehouse.py     # products + opening stock from Excel (one-time)
/tmp/bfs-venv/bin/python3 scripts/import_qb_data.py all  # stock + sales from QB exports (one-time bootstrap)
```

Stock quantities are now maintained by the **nightly QB API sync** (`/api/cron/sync`). Product names are overwritten monthly by the **name sync cron** (`/api/cron/sync-names`). Do not re-run these import scripts in production — they would overwrite live data.

---

## Current Data State (as of 2026-07-20)

- **901 active products** — Inverness's 81 products are active and warehoused-B2B like everything else (see feature 24); they're distinguished only by `Brand.isWarehoused = false` (feature 53), not `Product.isActive`
- **862 stock records** — live via nightly QB Items API sync
- **3,477 monthly sales records** from QB Sales Detail
- **569 products** have calculated reorder points and reorder quantities
- **Local brands** (Beauty First, Desembre, Fernanda's, Refectocil) → 2-month stock target, 14-day lead time
- **International brands** (all others) → 6-month stock target, 45-day lead time
- **Disabled locations**: BF Inverness, BLX Inverness (direct-supply, `isActive = false`) — a `Location`-level flag, unrelated to the `Brand.isWarehoused` flag above
- **QB Integration**: Production OAuth connected (Realm ID: 123145858555379), refresh token valid until ~Sep 2026
- **Dispatch movements**: 11,280 `ADJUSTMENT_OUT` records — 10,405 (92%) with QB invoice reference + store attribution

---

## Feature Status

### ✅ Done

| #   | Feature                                          | Key files                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                   |
| --- | ------------------------------------------------ | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | --- |
| —   | UI Shell                                         | `components/app-header.tsx`, `components/app-sidebar.tsx`, `app/(dashboard)/layout.tsx`, `lib/font.ts`                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                      |
| 1–4 | Products, Brands, Categories, Dashboard          | `components/products/`, `components/dashboard/`, `app/api/products/`                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                        |
| 5   | Stock & Locations                                | `app/api/stock/`, `app/api/locations/`, `components/stock/`                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                 |
| 6   | Reorder Management                               | `app/api/reorder/`, `components/reorder/`                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                   |
| 7   | Supplier Management                              | `app/api/suppliers/`, `components/suppliers/`                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                               |
| 8   | Purchase Orders                                  | `app/api/purchase-orders/`, `components/purchase-orders/`                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                   |
| 9   | Email Notifications                              | `app/api/notifications/`, `lib/mailer.ts`, `lib/notification-engine.ts`, `components/notifications/`                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                        |
| 10  | Sales History / Analytics                        | `app/api/sales/`, `components/analytics/`                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                   |
| 11  | Reports                                          | `app/api/reports/`, `components/reports/`                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                   |
| 12  | Import / Export                                  | `app/api/import/`, `app/api/export/`, `components/import-export/`                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                           |
| 13  | QB Integration                                   | `app/api/integrations/quickbooks/`, `hooks/use-integrations.ts`, `components/integrations/`                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                 |
| 14  | Zenoti Fulfillment (Phase 1)                     | `app/api/zenoti/`, `hooks/use-zenoti.ts`, `components/zenoti/` — see section below                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                          |
| 15  | Settings                                         | `app/api/settings/stock-policy/`, `components/settings/`                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                    |
| 16  | Authentication + RBAC                            | `lib/auth.ts`, `lib/auth-client.ts`, `lib/require-role.ts`, `components/auth/login-view.tsx`, `middleware.ts`                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                               |
| 17  | User admin panel                                 | `app/api/users/`, `hooks/use-users.ts`, `components/settings/dashboard/user-table.tsx` — Team tab in Settings                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                               |
| 18  | QB refresh token alert                           | `lib/qb-token-check.ts` — runs in nightly cron, emails admin if ≤7 days                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                     |
| 19  | Empty states                                     | `components/ui/empty-state.tsx` — used in products, stock, movements, email log, suppliers, reorder tables                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                  |
| 20  | QB Vendor sync                                   | `app/api/integrations/quickbooks/vendors/route.ts`, Vendors tab in Integrations                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                             |
| 21  | QB Product name sync                             | `app/api/integrations/quickbooks/items/sync-names/route.ts`, monthly cron `0 7 1 * *`                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                       |
| 22  | Product detail page                              | `components/products/view/product-view.tsx` — image, sales bar chart, stock balance line chart, recent movements table                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                      |
| 23  | Product image upload                             | UploadThing endpoint `productImage` in `app/api/uploadthing/core.ts`; CDN at `utfs.io` (added to `next.config.ts` remotePatterns)                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                           |
| 24  | QB product active-status sync (bidirectional)    | Stock sync (`POST /api/integrations/quickbooks/items`) treats QBO `Active` as fully authoritative over `Product.isActive`. Main loop (active QBO items) reactivates any matched BFS product that was `isActive = false`. Second pass (inactive QBO items) deactivates matching BFS products. Both directions match via `resolveProduct()`: SKU → barcode → exact name → hierarchy-stripped name — so QB items with no SKU still match by name. No more special-casing for non-warehoused/direct-supply products (e.g. Inverness) — accounts' QBO cleanup is now the single source of truth for status, and BFS operates as a B2B middleman for all products via Shopify regardless of warehousing.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                          |
| 25  | Sales calc shared helper                         | `lib/sales-calc.ts` — `computeAvgMonthly()` with trailing-zero trimming + linear recency weighting + `confident` flag. Imported by reorder route, calculate-minimums route, and stock-policy route.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                         |
| 26  | Email XLSX brand-per-sheet                       | `lib/email-xlsx.ts` rewritten — one sheet per brand (alphabetical), both Out-of-Stock and Low-Stock items together per sheet, Status column with red/amber coloring, Summary sheet with brand breakdown.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                    |
| 27  | QB stock sync delta tracking                     | `POST /api/integrations/quickbooks/items` now reads existing inventory qty before upserting. Writes `ADJUSTMENT_OUT` when QB qty drops (dispatched), `ADJUSTMENT_IN` when it rises (restocked), nothing when unchanged. `RECONCILIATION` is now only written for a product's first-ever sync (opening snapshot). Response includes `movements: { dispatched, restocked, unchanged }` counts.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                |
| 28  | QB invoice backfill (done, button removed)       | `POST /api/integrations/quickbooks/backfill-movements` — read-only one-time route that reconstructed 2 years of `ADJUSTMENT_OUT` movements from QB Invoices. 10,405 movements written with `reference = "QB-INV-{DocNumber}"` and `notes = "QB backfill: invoiced to {store}"`. Route and UI button retained in codebase but button removed from Integrations UI after confirming data (10,405/10,435 = 99.7% attributed).                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                  |
| 29  | Movements Log — brand filter + By Product view   | Brand dropdown filter. Summary cards (Received / Dispatched / Net change) when date range active. **By Product** toggle: one row per product with `totalIn`, `totalOut`, `netChange`, `currentStock`, `movementCount`. `GET /api/stock/movements/summary` uses `$queryRaw`. `RECONCILIATION` and `OPENING_STOCK` excluded from both totals.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                 |
| 30  | Movements export — Excel download                | **Export Excel** button on By Product view. `GET /api/stock/movements/summary/export` — same filters as summary, ExcelJS output with coloured Total In/Out/Net columns, meta rows (brand, period), totals row. Filename: `movements-{dateFrom}-{dateTo}.xlsx`.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                              |
| 31  | QB unit cost sync                                | During stock sync loop, if `item.PurchaseCost != null && > 0`, calls `updateMany` on `ProductSupplier` for that product. `costUpdated` count in sync response and SyncLog message.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                          |
| 32  | QB invoice attribution on movements              | Before the sync loop in `POST /api/integrations/quickbooks/items`, fetches QB Invoices since `lastSyncAt` (default 48h on first run). Builds `Map<qboItemId, [{docNumber, customerName}]>`. `ADJUSTMENT_OUT` movements now get `reference = "QB-INV-1023"` and `notes = "QBO sync: dispatched to Square One"`. Non-fatal if invoice fetch fails — movements still written without attribution.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                              |
| 33  | Pending product staging workflow                 | `PendingProduct` table (migration `20260622180459_add_pending_product`). QB sync upserts unmatched items here instead of silently skipping. `/products/pending` page: table sorted by qty desc, with QB name, SKU, cost, suggested brand (parsed from QB hierarchy), first seen, seen count. **Add** button opens Sheet to validate name/SKU/barcode/brand/category/location → creates Product + Inventory + `OPENING_STOCK` movement + deletes pending record. **Dismiss** removes from list (reappears on next sync if still unmatched). Sidebar: Products now has sub-items (All Products / Pending from QB). API: `GET/DELETE /api/products/pending`, `POST /api/products/pending/[id]/approve`.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                        |
| 34  | Restocks / Dispatches preset views               | Movements Log now has three preset chips: **All Movements** / **Restocks** / **Dispatches**. Each sets the type filter (movements view) and `typeGroup` param (By Product view). Summary API supports `typeGroup=in\|out` to scope SQL to IN or OUT types only. By Product view in Restocks mode shows: Units Restocked, Restock Events, Avg per Restock, Current Stock, Last Restock. Dispatches mode mirrors with OUT columns.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                            |
| 35  | Dispatch by Store report                         | New tab in Reports → **Dispatch by Store**. `GET /api/reports/dispatch-by-store` parses store name from `notes` field of `ADJUSTMENT_OUT` movements, pivots product × store. Table: product rows, store columns (sorted alpha, Unknown last), per-store totals sub-header, Total + Unit Cost + Total Value columns. ExcelJS export with frozen headers and totals row. Filter: date range + brand.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                          |
| 36  | Internal Use & Shrinkage tracking                | `INTERNAL_USE` added to `StockMovementType`. `isReviewed`, `reviewNote`, `reviewedAt` added to `StockMovement` (migration `20260622205144`). **Proactive path**: "Log Internal Use" sheet on Movements page (product combobox, location, qty, reason, notes) → `POST /api/stock/adjust` type=INTERNAL_USE. **Reactive path**: "Shrinkage" preset chip shows unreviewed `ADJUSTMENT_OUT` with no QB reference; "Explain" button → `PATCH /api/stock/movements/[id]/review`. QB sync reconciles INTERNAL_USE delta before writing ADJUSTMENT_OUT to avoid double-counting. `INTERNAL_USE` subtracts from stock but is excluded from OUT totals in summary SQL.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                |
| 37  | ProductCombobox + minimal products API           | `components/ui/product-combobox.tsx` — searchable Popover+Command, filters by name + SKU + brand. `GET /api/products?minimal=true` bypasses pagination, returns all active products as `[{id, name, sku, brandName}]`. `useProductsMinimal()` hook (5-min stale). Used in Log Internal Use sheet and New PO line items. Use this for all product picker fields — plain Select can't handle 901 products.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                    |
| 38  | Prettier + Husky pre-commit/pre-push             | `.prettierrc` with `trailingComma: "es5"`, `useTabs: true`, `tabWidth: 4`. `pnpm format`, `pnpm format:check`, `pnpm typecheck` scripts. Husky v9: pre-commit runs `lint-staged` (prettier + eslint --fix on staged files), pre-push runs `tsc --noEmit`. ESLint config tuned: `no-explicit-any` → warn, `ban-ts-comment` off for chart.tsx/product-view.tsx/lib/auth.ts, `set-state-in-effect` off for use-mobile.ts and fulfillment-view.tsx.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                             |
| 39  | Zenoti Excel import + email notifications        | `lib/zenoti-excel.ts` — `parseOrderFile()` + `upsertZenotiOrder()`. `app/api/zenoti/import-excel/route.ts` (POST, multipart). `app/api/zenoti/scan-uploads/route.ts` (POST scans `uploads/zenoti/{org}/`). `components/zenoti/import/zenoti-import-panel.tsx` — drag-drop + scan. `components/zenoti/dashboard/zenoti-page-tabs.tsx` — tabs (Procurement Orders / Import from Excel). `lib/zenoti-email.ts` — `sendZenotiImportEmail()` fires fire-and-forget on each newly-created order. Styled HTML email per supplier type with unmatched product codes listed. **All emails currently route to `order@beautylogix.ca` for testing** — see `TYPE_META` in `lib/zenoti-email.ts` to restore production routing. `ZenotiOrder.zenotiOrderId` for Excel imports uses format `MANUAL-{ORG}-{orderNumber}`.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                  |
| 40  | Zenoti supplier type classification              | `ZenotiOrder.supplier String?` field added (migration `20260625182009_add_zenoti_order_supplier`). `lib/zenoti-email.ts` exports `SupplierType` (`WAREHOUSE \| COSTCO \| INVERNESS \| OTHER`) and `getSupplierType()` (matches supplier name substring). Dashboard: "Org" column replaced by "Type" badge (emerald/blue/violet/gray). Filter chips: All / Warehouse / Costco / Inverness. UI falls back to parsing `notes` field for orders imported before the migration.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                  |
| 41  | iPad-friendly packing UI                         | `components/zenoti/view/fulfillment-view.tsx` — `PackingCard` component replaces table rows. Each card: `QtyStepperField` (−/+ buttons + blur-to-save input), notes `Textarea` (700ms debounced save via `useRef`), packed checkbox (green card when checked). State synced from server via `useEffect` (ESLint `set-state-in-effect` override added for this file in `eslint.config.mjs`).                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                 |
| 42  | PDF packing slip                                 | `lib/packing-slip-pdf.tsx` — `@react-pdf/renderer` v4 document. Built-in Helvetica/Helvetica-Bold (no network font fetch). Beauty First brand pink `#d4006e` accent. A4 layout: logo header, 5-col meta cards, status pills, colour-coded items table (green/amber/red/violet rows), totals, legend, sign-off blocks. `wrap={false}` on legend + sign-off prevents mid-box page breaks. `GET /api/zenoti/fulfillments/[id]/packing-slip` streams PDF. Submit route generates PDF + XLSX in `Promise.all` and attaches both. "Download PDF" button in fulfillment UI. Logo: `public/assets/images/Beauty_Logix_Logo.png` (converted from AVIF via `sips`). `@react-pdf/renderer` in `serverExternalPackages`.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                |
| 43  | QB Sales API sync button removed                 | `SalesByProductServiceSummary` endpoint returns 403 (QB user lacks Reports permission) and was already removed from the nightly cron. The broken "Sync from QuickBooks" card removed from the Sales tab in `components/integrations/dashboard/integrations-dashboard.tsx`. CSV paste fallback (`QbSalesImport`) remains on the Sales tab.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                   |
| 44  | Configurable email recipients via Settings       | `AppSetting` key-value store used for all Zenoti email addresses. `GET/PATCH /api/settings/email-recipients` — six keys: `zenoti_email_warehouse`, `zenoti_email_costco`, `zenoti_email_inverness`, `zenoti_email_other`, `zenoti_email_packing_list_to`, `zenoti_email_packing_list_cc`. `lib/email-recipients.ts` — `getEmailRecipients()` async helper (reads AppSetting, merges with defaults). `lib/zenoti-email.ts` `TYPE_META` now has `settingKey` instead of hardcoded `to`. Submit route reads recipients via `getEmailRecipients()`. Settings → Email Recipients tab: `components/settings/dashboard/email-recipients-form.tsx` — two sections (Import notifications 2×2 grid, Packing list To/CC). `lib/mailer.ts` now accepts optional `cc` field. Do not import from the route file — use `lib/email-recipients.ts`.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                          |
| 45  | Decoupled email from order completion            | Submit route (`POST /fulfillments/[id]/submit`) now only marks `SUBMITTED` — no PDF, no email. New explicit routes: `POST /api/zenoti/fulfillments/[id]/send-email` (generates PDF + XLSX, emails packing list to accounting); `POST /api/zenoti/orders/[id]/notify` (supplier-type-aware notification email with order details table + "View Order →" link, colour-coded by type, reads recipient from AppSetting). `GET /api/zenoti/orders` now includes `fulfillment.id` in select. Fulfillment view: "Submit & Email" → "Mark Complete" (AlertDialog, just locks order) + "Actions" `DropdownMenu` (Download Packing Slip, Send Packing List Email, Send Order Notification). Orders table: ellipsis `DropdownMenu` per row with same actions; row click still navigates. Hooks: `useSendPackingListEmail()`, `useSendOrderNotification()`.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                             |
| 46  | Responsive design polish (8 tasks)               | `app/(dashboard)/layout.tsx` — `min-w-0` on `SidebarInset` activates built-in `overflow-x-auto` on all 15 tables simultaneously. `app/layout.tsx` — `export const viewport: Viewport` (Next.js App Router standard). `components/layout/breadcrumbs.tsx` — parent crumbs hidden on mobile (`hidden sm:flex`), current page label truncates (`max-w-[14rem] truncate`). `components/app-header.tsx` — left side gets `min-w-0 flex-1`, trigger + separator get `shrink-0`. Dashboard grid: `lg:grid-cols-4` on KPI row; chart grid `md:grid-cols-2 lg:grid-cols-3`. Form grids: all 5 sheet forms (`product-form`, `supplier-form`, `po-form`, `pending-products`, walk-in dialog) changed `grid-cols-2` → `grid-cols-1 sm:grid-cols-2`. Movements summary cards `grid-cols-1 sm:grid-cols-3`. Login `h-screen` → `min-h-dvh`.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                               |
| 47  | Email / document design standards                | All HTML emails upgraded to `<!DOCTYPE html><html lang="en">` wrapper (bare `<div>` breaks Outlook). `lib/email-templates.ts` rewritten: `base()` now takes `headerBg` param; each alert type has a distinct colour header (red / amber / purple / green / dark navy). Emoji icons (`⚠️📦🚨✅`) removed — replaced with coloured text labels. Table `<th>` styled with uppercase + letter-spacing. `lib/zenoti-email.ts` + `app/api/zenoti/orders/[id]/notify/route.ts` — same DOCTYPE fix + uniform label style. Packing list email (`send-email/route.ts`) upgraded from plain `<p>` list to full branded card with `#d4006e` header + summary table + "View in BFS Inventory →" CTA. XLSX packing list — freeze row added (`ySplit: 5`). Stock alert XLSX Summary sheet — freeze row added. PDF packing slip was already production quality — no changes.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                |
| 48  | Shopify integration (Phase 1)                    | OAuth connect flow: `GET /api/integrations/shopify/connect?shop=` → Shopify consent → `GET /api/integrations/shopify/callback` (HMAC verified, no shop cookie check) → token stored in `IntegrationConfig.config.stores[]` array (provider `SHOPIFY`, `@unique`). Multi-store: DB OAuth tokens merged with env var fallback (`SHOPIFY_STORE_N_DOMAIN` + `SHOPIFY_STORE_N_TOKEN`, up to 5). Two stores live: `ctyvhx-0k.myshopify.com` (beauty-first-spa-wholesale, OAuth) + `7f7b80-5d.myshopify.com` (beautylogix, OAuth — different Shopify org, same Partners app). Order sync: `POST /api/integrations/shopify/sync` — fetches `status=open` orders + second pass updates orders that dropped out of the unfulfilled feed (fetches individual order by ID, guards against empty `notIn` array in Prisma). Inventory push: `POST /api/integrations/shopify/sync-inventory` — maps BFS `sku`/`barcode` → Shopify variant SKU, 600ms rate-limit throttle, per-variant error catching, `read_locations` scope required. Fulfillment status updates on sync; stale errors surfaced in toast. Store management API: `GET/DELETE /api/integrations/shopify/stores`. Integrations → Shopify tab: connect form, per-store Sync Orders + Push Stock buttons, disconnect AlertDialog. Orders table (`/shopify`): all orders shown (not just unfulfilled), Payment badge + Fulfillment badge (green/amber/blue), fulfillment filter dropdown, optional `shop` filter. Crons: weekly inventory push Mon 07:00 UTC (`/api/cron/sync-shopify-inventory`, maxDuration 300s), nightly order sync Mon–Fri 07:30 UTC (`/api/cron/sync-shopify-orders`). `lib/shopify.ts`: `getConnectedStores()`, `getShopifyStores()`, `fetchShopifyOrders()`, `fetchShopifyOrder()`, `fetchShopifyProducts()`, `fetchShopifyLocations()`, `setInventoryLevel()`, `customerName()`. `hooks/use-shopify.ts`, `hooks/use-shopify-stores.ts` (includes `useSyncShopifyOrdersForStore()`, `useSyncShopifyInventoryForStore()`). `components/integrations/dashboard/shopify-connect.tsx`, `components/shopify/dashboard/shopify-orders-table.tsx`.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                             |
| 49  | QB brand/category sync from ParentRef/ClassRef   | `QboItem` (`lib/qbo.ts`) adds optional `ParentRef` (item's parent in QBO's item hierarchy → brand) and `ClassRef` (QBO Class → category). `POST /api/integrations/quickbooks/items` resolves both per item via `resolveBrandId()`/`resolveCategoryId()` (find-or-create by case-insensitive name, request-scoped cache) and updates `Product.brandId`/`categoryId` only when QBO supplies the field and it differs from the current value — items accounts hasn't classified yet in QBO are left untouched. Runs in the same per-item transaction as the reactivation/inventory update. Response + SyncLog message include `brandUpdated`/`categoryUpdated` counts.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                         |
| 50  | Shopify brand/category push (Push Stock)         | `POST /api/integrations/shopify/sync-inventory` now also pushes brand/category to Shopify's product-level `vendor`/`product_type` fields (Shopify has no per-variant equivalent). `fetchShopifyProducts()` (`lib/shopify.ts`) fetches `vendor`/`product_type` alongside `id`/`variants`; `updateShopifyProduct()` (renamed from `updateShopifyProductClassification()` in feature 55, which extended it to also handle `status`) does a `PUT /products/{id}.json`. Per Shopify product, resolves `brandName`/`categoryName` from the first variant SKU that matches a BFS product with a set `brand`/`category`, then writes only the fields that are both present in BFS and different from what Shopify already has — products QBO/BFS haven't classified yet leave Shopify's existing `vendor`/`product_type` untouched. One extra API call per product needing an update, same 600ms throttle as inventory/price pushes. Response adds `classificationSynced` count per store; UI (`shopify-connect.tsx`) shows it in the Push Stock result panel. Verified live against `ctyvhx-0k.myshopify.com`: 284 products updated, 0 errors, confirmed idempotent (already-correct vendor/product_type left alone) and additive-only (unclassified BFS products don't touch Shopify's existing values).                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                          |
| 51  | Shopify order discount capture                   | `ShopifyOrder` gains `totalDiscounts`/`discountCodes`; `ShopifyOrderItem` gains `totalDiscount`. `ShopifyApiOrder`/`ShopifyApiLineItem` (`lib/shopify.ts`) add `total_discounts`, `discount_codes`, `total_discount` — captures the actual per-customer discount (franchise stores have reusable discount codes) rather than just list price. Populated in `POST /api/integrations/shopify/sync`.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                           |
| 52  | Shopify order fulfillment (packing workflow)     | Zenoti-equivalent packing workflow for Shopify orders, since Zenoti is being phased out (no API keys — see backlog) and ordering is moving to Shopify. New models `ShopifyFulfillment`/`ShopifyFulfillmentItem` (1:1 with `ShopifyOrder`, mirrors `BfsFulfillment`/`BfsFulfillmentItem`). `POST /api/shopify/orders/[id]/fulfillment` creates the shell, matching Shopify line item SKUs to BFS `Product` and pre-loading `fulfilledQty` with the ordered qty. `PATCH /api/shopify/fulfillments/[id]/items/[itemId]` updates qty/notes/packed. `POST /api/shopify/fulfillments/[id]/submit` locks it (`Mark Complete`). `GET /api/shopify/fulfillments/[id]/packing-slip` streams a PDF via `lib/shopify-packing-slip-pdf.tsx` (separate from `lib/packing-slip-pdf.tsx` — Shopify orders show per-line unit price/discount/net + order subtotal/discount/total instead of Zenoti's retail/consumable split, and have no walk-in items). `POST /api/shopify/fulfillments/[id]/send-email` emails the PDF to accounts (`shopify_packing_list_to`/`_cc` — no XLSX, unlike Zenoti). UI: `components/shopify/view/shopify-order-view.tsx` extended in place — pre-fulfillment shows the existing read-only items+stock table with a "Start Packing" button; once a fulfillment exists it switches to `PackingCard` rows (single qty stepper, not retail/consumable) plus a progress bar, "Mark Complete", and an "Actions" dropdown (Download Packing Slip, Send Packing List Email, and — only when the order has non-warehoused items, see feature 53 — Send Non-Warehoused Notification). Hooks in `hooks/use-shopify.ts` (not a separate file). **Auto-backfill for already-Shopify-fulfilled orders**: if `order.fulfillmentStatus === 'fulfilled'` and no local `ShopifyFulfillment` exists yet (order shipped without ever going through BFS packing — common for anything fulfilled before this feature existed, or fulfilled directly in Shopify), `shopify-order-view.tsx` fires `POST /api/shopify/orders/[id]/fulfillment` automatically on mount via a `useEffect` (guarded by a `useRef` so it only fires once per mount). The creation route detects this case and creates the record pre-packed (`isPacked: true`, `fulfilledQty = requestedQty`) and pre-submitted (`status: 'SUBMITTED'`, `submittedBy: 'Synced from Shopify'`) instead of `IN_PROGRESS`, so "Start Packing" is never offered for something already gone and the Actions menu (download/email packing slip) works retroactively. "Start Packing" itself is gated on `!f && !shopifyFulfilled` so it never renders during the brief window before the backfill completes. The route also catches `P2002` (unique constraint on `orderId`) and returns the existing record instead of erroring, since the effect can theoretically double-fire. |
| 53  | Non-warehoused item flagging (generalized)       | Originally hardcoded to `brand.name === 'inverness'`; generalized to a real `Brand.isWarehoused` column (`Boolean @default(true)`) so any brand can be flagged as not-stocked-in-house without a code change — just toggle it in Settings → Brand Lead Times → new "Stock Location" column (`Switch` per row, `PUT /api/brands/[id]` accepts `isWarehoused`). Backfill migration set `Inverness`/`INVERNESS` (both casing variants, 81 products combined) to `false`; everything else defaults `true`. Detection is `product.brand?.isWarehoused === false`, computed live wherever fulfillment items are returned/rendered (order route, packing-slip route, send-email route, notify route) — never stored as a column on the item itself. Packing cards show a violet "Not in-house" badge. Manual "Send Non-Warehoused Notification" action in the Actions dropdown (only rendered when the order has ≥1 such item) → `POST /api/shopify/fulfillments/[id]/notify-non-warehoused` (renamed from `notify-inverness`), emails `shopify_non_warehoused_notify_to` (renamed from `shopify_inverness_notify_to` — DB row migrated, not just added) listing the flagged SKUs grouped with their brand name(s) so accounts can source/invoice them separately. Server-side 400s if called with zero non-warehoused items on the order. **Do not confuse with `zenoti_email_inverness`** — that's an unrelated, still-valid Zenoti purchase-order supplier-type classification (WAREHOUSE/COSTCO/INVERNESS/OTHER), untouched by this generalization.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                            |
| 54  | Shopify new-order → check-QBO notification       | `sendShopifyNewOrdersEmail()` (`lib/shopify-email.ts`) already existed and already fired from `POST /api/integrations/shopify/sync` on new orders, but was a silent no-op — it read `shopify_order_notify_to`/`_cc` via a raw `prisma.appSetting.findMany` with no default fallback, and no `AppSetting` row existed, so `toSetting` was always `''` and the send was skipped. Fixed to use the shared `getEmailRecipients()` helper (`lib/email-recipients.ts`), which already has the correct default (`order@beautylogix.ca`) baked into `RECIPIENT_DEFAULTS`. Copy updated to explicitly say the order syncs to QuickBooks via the native QBO Shopify app and to check QuickBooks for the invoice — BFS Inventory does not create it. `AppSetting` rows for `shopify_order_notify_to`, `shopify_non_warehoused_notify_to` (renamed from `shopify_inverness_notify_to` in feature 53), `shopify_packing_list_to` explicitly set to `order@beautylogix.ca` (testing default) — all three configurable via Settings → Email Recipients, which gained three new field groups mirroring the existing Zenoti sections.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                        |     |
| 55  | Active/draft status push to Shopify (Push Stock) | `POST /api/integrations/shopify/sync-inventory` now also pushes `Product.isActive` → Shopify's product-level `status` field (`active`/`draft`) — this was previously believed to exist but never actually did; only the QBO↔BFS `isActive` sync (feature 24) existed, nothing pushed onward to Shopify. Pre-fetch query changed from `prisma.inventory.findMany` (active products only) to `prisma.product.findMany` covering **both** active and inactive products with a SKU/barcode, so inactive products can still be matched (`isActiveBySku` map) even though their stock/brand/category are skipped as before. **Status uses "any variant active wins," not "first match wins"** like brand/category — deliberately different, because a Shopify product bundling multiple BFS products as variants (e.g. different pack sizes under one listing) must stay `active` if even one variant is still active, or the whole listing wrongly disappears from the storefront over an unrelated discontinued variant. Caught and fixed live: SKU `111100000317` (active) shared a Shopify product with `8809057513257` (inactive); first-match-wins picked the inactive one and wrongly drafted the whole listing until switched to any-active-wins. `updateShopifyProduct()` (renamed from `updateShopifyProductClassification()`) now accepts `status` alongside `vendor`/`product_type` in the same `PUT` call — no extra API request. Response/UI/SyncLog gained a `statusSynced` count alongside `classificationSynced`. Verified live against both stores: 71 status changes, 0 errors on the first run (one bug found: the multi-variant case above); after the any-active-wins fix, a full re-scan of all products confirmed zero remaining mismatches in either direction.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                          |

### ⏳ Zenoti Phase 2 (on hold — likely deprecated)

**As of 2026-07: Zenoti is expected to be phased out entirely.** Ordering is migrating to Shopify (see feature 52, the Shopify order fulfillment workflow built as the Zenoti-fulfillment replacement) and leadership is not pursuing the Zenoti API keys needed to unblock live sync below. The Zenoti fulfillment code (`ZenotiOrder`, `BfsFulfillment`, `/zenoti` routes/UI) is left in place and untouched — not actively deleted — but should be treated as legacy. Do not invest further work here without explicit direction; the table below is kept for historical context only.

| Item                     | Blocker                                                                                                                                                 | Notes                                                                                                                                                                                      |
| ------------------------ | ------------------------------------------------------------------------------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ |
| Live sync active         | API subscription not yet enabled — ticket **BC-60590** raised 2026-06-18, ETA ~2026-06-23 (3–4 business days). Registered email: alvinkigen@outlook.com | Once activated, get API key from Zenoti Admin → Setup → Apps, add `ZENOTI_BFS_API_KEY` + `ZENOTI_BL_API_KEY` to `.env` + Vercel, then hit `GET /api/zenoti/centers` to discover center IDs |
| QB Invoice posting       | Confirm accounting team sign-off + QB customer names per store                                                                                          | `POST /api/zenoti/fulfillments/[id]/post-to-qb` — next step after fulfillment is signed off; QB OAuth already wired in                                                                     |
| Confirm PO endpoint path | Need real API key to test                                                                                                                               | Current assumption: `GET /v1/procurement/purchase_orders?center_id=&statuses=Raised,Updated` — verify once keys arrive, adjust `lib/zenoti.ts` `fetchZenotiPOs` if path differs            |
| Restore email routing    | Currently all import emails go to `order@beautylogix.ca` for testing                                                                                    | Update `TYPE_META` in `lib/zenoti-email.ts`: COSTCO + INVERNESS → `accounting@beautyfirstspa.com`; confirm OTHER recipient with team. Use Email Recipients tab in Settings to update live. |

---

## QuickBooks Integration — Full Picture

QB is the **source of truth** for stock quantities and product names. All file-upload/CSV-paste routes are kept for emergency manual correction only.

### Sync modes

| Mode                  | Route                                                | Trigger                  | What it does                                                                                                                                                                                                                                                                                                                                               |
| --------------------- | ---------------------------------------------------- | ------------------------ | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| **Live stock sync**   | `POST /api/integrations/quickbooks/items`            | UI button / nightly cron | Fetches `QtyOnHand` from QB Items API → upserts inventory. Writes `ADJUSTMENT_OUT`/`ADJUSTMENT_IN` for deltas; `RECONCILIATION` only on first-ever sync. Attributes `ADJUSTMENT_OUT` to QB invoice number + store name via invoice fetch before the loop. Updates `ProductSupplier.cost` from `PurchaseCost`. Upserts unmatched items to `PendingProduct`. |
| **Live sales sync**   | `POST /api/integrations/quickbooks/sync-sales-api`   | UI button / nightly cron | Fetches `SalesByProductServiceSummary` (last 12 months) → upserts SalesRecord                                                                                                                                                                                                                                                                              |
| **Product name sync** | `POST /api/integrations/quickbooks/items/sync-names` | UI button / monthly cron | Overwrites `product.name` from QB canonical name, matched by SKU only                                                                                                                                                                                                                                                                                      |
| **Vendor sync**       | `POST /api/integrations/quickbooks/vendors`          | UI button (on-demand)    | Imports active QB Vendors → upserts Suppliers; fills blank fields only, never overwrites manual edits                                                                                                                                                                                                                                                      |
| **XLS file import**   | `POST /api/integrations/quickbooks/sync-stock`       | UI file upload           | Emergency: parse `ProductServiceList__*.xls` → upsert inventory                                                                                                                                                                                                                                                                                            |
| **CSV paste import**  | `POST /api/integrations/quickbooks/sync-stock`       | UI text area             | Emergency: rows from CSV paste                                                                                                                                                                                                                                                                                                                             |
| **Sales CSV import**  | `POST /api/integrations/quickbooks/sync-sales`       | UI text area             | Emergency: QB Sales Detail CSV (flat or wide format)                                                                                                                                                                                                                                                                                                       |

### Cron jobs

| Schedule                           | Route                                  | What it does                                                                         |
| ---------------------------------- | -------------------------------------- | ------------------------------------------------------------------------------------ |
| `0 6 * * *` (daily 06:00 UTC)      | `GET /api/cron/sync`                   | Stock sync + QB token expiry check (sales sync removed — QB Reports API returns 403) |
| `0 7 1 * *` (monthly, 1st)         | `GET /api/cron/sync-names`             | Overwrites product names from QB by SKU match                                        |
| `0 7 * * 1` (Monday 07:00 UTC)     | `GET /api/cron/sync-shopify-inventory` | Push BFS stock levels to all connected Shopify stores (maxDuration 300s)             |
| `30 7 * * 1-5` (Mon–Fri 07:30 UTC) | `GET /api/cron/sync-shopify-orders`    | Pull orders from all connected Shopify stores, update fulfillment statuses           |

Both cron routes are protected by `Authorization: Bearer <CRON_SECRET>`. Vercel sends this header automatically; set `CRON_SECRET` in both `.env` and Vercel environment variables.

The middleware and `requireRole` both accept the `Bearer <CRON_SECRET>` header so cron sub-fetches bypass session auth cleanly.

### Sales sync — quantity handling

`SalesByProductServiceSummary` may return only revenue columns (no unit qty) depending on QB report config. The sync route detects this via `qtyColumnsDetected` in the response. When `false`, existing `SalesRecord.quantity` values (from the Excel bootstrap) are preserved — the upsert only overwrites quantity when QB actually returns a non-zero qty. Revenue is always updated.

**Sales sync is not in the cron** — QB OAuth user lacks Reports permission (403). The bootstrapped 12 months of data is sufficient for reorder calculations.

### Name sync — matching logic

Only items with a QB SKU are renamed. Matching priority: `product.sku` → `product.barcode`. Items matched by barcode have `product.sku` back-filled for future runs. Items with no QB SKU are counted as `noSku` and skipped to avoid ambiguous matches. The diff (old name → new name) is returned in the response and logged to SyncLog with type `NAME_SYNC`.

### Shared QB helpers in lib

- `QboItem` interface — `lib/qbo.ts`
- `fetchQboItems(activeOnly = true)` — paginated fetch of QB Inventory items — `lib/qbo.ts`. Pass `false` to fetch inactive items (used by the deactivation pass in the stock sync route).
- `QboInvoice` / `QboInvoiceLine` interfaces + `fetchQboInvoices(fromDate: string)` — paginated fetch of QB Invoices since a given date — `lib/qbo.ts`. Used by the backfill route.
- **Do not import types or functions from route files into other route files.** Next.js App Router bundles each route independently; cross-route imports cause 502s. Put shared QB types/helpers in `lib/qbo.ts`.

### OAuth

Tokens live in `IntegrationConfig.config.oauth` (JSON). Access token auto-refreshes when <2 min from expiry. Refresh token expires ~Sep 2026 — if it expires, reconnect via `/integrations` → Settings tab. The nightly cron emails the admin (`GMAIL_USER`) if fewer than 7 days remain.

**QB redirect URI** (registered in Intuit portal):

```
https://bfs.kigtech.digital/api/integrations/quickbooks/callback
```

**Local dev**: shared Neon DB means local dev uses the same tokens as prod once QB is connected. Set `QBO_ENVIRONMENT=production` in `.env`.

---

## Authentication & RBAC

**Current state**: GitHub + Google OAuth. ADMIN/MANAGER/VIEWER roles enforced on all write routes.

- Login: `app/(auth)/login/page.tsx`
- better-auth config: `lib/auth.ts` (socialProviders: github + google)
- Middleware allowlist: `middleware.ts` — specific emails + `@beautylogix.ca` / `@beautyfirstspa.com` domains
- Session: 7-day expiry, 24-hour sliding update
- Role helper: `lib/require-role.ts` — `requireRole("ADMIN" | "MANAGER")`, returns `{ user }` or `NextResponse` 401/403

**Role split**:

- `ADMIN` — settings, locations, user role management, QB connect/disconnect, product/supplier/stock imports
- `MANAGER` — products, brands, categories, suppliers, purchase orders, stock adjustments, notifications, QB syncs, Zenoti fulfillment
- `VIEWER` — read-only (all GET routes are public to any authenticated session)

**User admin**: Team tab in `/settings` (`components/settings/dashboard/user-table.tsx`). Role selector for others, read-only badge for self. Self-demotion blocked at API level.

**First admin**: must be set directly in DB (`UPDATE "user" SET role = 'ADMIN' WHERE email = '...'`). All new sign-ins default to `VIEWER`.

**What's still missing**:

- No email/password fallback — if Google/GitHub OAuth is down, no login path exists.

**Legal pages** (public, no auth required):

- `https://bfs.kigtech.digital/legal/privacy`
- `https://bfs.kigtech.digital/legal/terms`

---

## Settings Page

Three tabs at `/settings`:

**Stock Policy** (`/api/settings/stock-policy`):

- Per-product `targetStockMonths` (Int, default 6) — drives `reorderQty = ceil(avgMonthly × targetStockMonths)`
- Accordion grouped by brand; quick-set buttons (1/2/3/6/9/12 mo) per brand header
- Sticky save bar; "Recalculate" button calls `/api/inventory/calculate-minimums`
- Note: `reorderPoint` on the reorder page is now computed live — "Recalculate" is only needed to refresh stored `inventory.reorderPoint`/`reorderQty` values used elsewhere (e.g. stock page thresholds)

**Brand Lead Times**:

- Editable `leadTimeDays` per brand (inline edit, Enter/Escape)
- Drives `reorderPoint = ceil(avgMonthly × (leadTimeDays + 7) / 30)`

**Team**:

- Lists all users with avatar, role selector, last seen, member since
- ADMIN-only API (`GET/PATCH /api/users`, `PATCH /api/users/[id]`)

---

## Reorder / Minimums Formulas

```
reorderPoint  = ceil(avgMonthly × (leadTimeDays + SAFETY_DAYS) / 30)   ← when to trigger an order
minQuantity   = ceil(avgMonthly × SAFETY_DAYS / 30)                    ← 1-week safety buffer
reorderQty    = ceil(avgMonthly × targetStockMonths)                    ← how much to order
```

`SAFETY_DAYS = 7` — defined in `lib/sales-calc.ts`.

### avgMonthly calculation — `computeAvgMonthly()` in `lib/sales-calc.ts`

Sales records are ordered DESC (most recent first). The function:

1. **Trims trailing zeros** — strips leading zero-quantity months before computing. These represent products on legal/distribution hold or extended zero-demand periods. The trim starts from index 0 (most recent) until a non-zero month is found.
2. **Linear recency weighting** — weight[i] = (n − i), so index 0 (most recent non-zero month) carries the highest weight. Trending-up products score higher than flat; trending-down score lower.
3. **`confident` flag** — `true` if ≥ 3 non-zero months remain after trimming. Reorder table shows `~{qty}` (greyed, with tooltip) when `!confident`.

Products with no non-zero sales after trimming return `{ avgMonthly: 0, monthsUsed: 0, confident: false }` and are skipped by calculate-minimums.

### Live vs stored reorderPoint

The reorder route (`GET /api/reorder`) computes `reorderPoint` live from `avgMonthly` and the brand's current `leadTimeDays`. The stored `inventory.reorderPoint` (set by Recalculate) is used as fallback only when `avgMonthly = 0`. This means changing a brand's lead time or a product's `targetStockMonths` is reflected immediately on the reorder page without running Recalculate.

---

## Zenoti Integration

**Status note (2026-07): likely being deprecated** — see **⏳ Zenoti Phase 2 (on hold — likely deprecated)** in the Backlog section below. Ordering is migrating to Shopify (feature 52's fulfillment workflow is the intended replacement) and leadership isn't pursuing the Zenoti API keys needed to unblock live sync. This section is kept accurate for whatever is still live, but treat it as legacy, not an active roadmap.

Zenoti is the inventory/POS system used at all service locations. Service locations raise Purchase Orders in Zenoti directed at the warehouse. BFS pulls those POs and the inventory associate fulfills them on an iPad, then emails a packing list to accounting.

**Zenoti cannot be updated from BFS** — the Zenoti API has no write endpoint for stock quantities. BFS cross-references its own live stock levels (from QB sync) to flag shortfalls at fulfillment time.

### Two Zenoti instances

| Domain                      | Label            | Env var              |
| --------------------------- | ---------------- | -------------------- |
| `beautyfirstspa.zenoti.com` | Beauty First Spa | `ZENOTI_BFS_API_KEY` |
| `beautylogix.zenoti.com`    | Beauty Logix     | `ZENOTI_BL_API_KEY`  |

Both use base URL `https://api.zenoti.com`. Auth: `Authorization: apikey {key}` header on every request. No OAuth, no token refresh.

**beautyfirstspa.zenoti.com — service locations:**

- Corp-Owned: Burlington Mall, Limeridge Mall, Oakville Place, Square One
- Franchise: Dixie Outlet Mall, Hillcrest Mall, Upper Canada Mall, Yonge-Eglinton
- Excluded: Call Center (not a store)

**beautylogix.zenoti.com — service locations:**

- Franchise: Bolton, Burlington, Milton, Oakville, Ottawa-Kanata, Rymal

### Fulfillment flow (as built)

```
1. Service location raises PO in Zenoti (RAISED or UPDATED status)
2. Inventory associate clicks "Sync from Zenoti" on /zenoti dashboard
   → POST /api/zenoti/sync pulls RAISED+UPDATED POs from all centers across both orgs
3. Associate clicks an order → /zenoti/[id]
   → "Start Packing" calls POST /api/zenoti/orders/[id]/fulfillment
   → Creates BfsFulfillment (status: IN_PROGRESS) with items pre-loaded from Zenoti,
     quantities defaulted to requested, products matched by barcode to BFS Product
4. Associate packs each item (iPad-optimised PackingCard UI):
   - −/+ stepper buttons + blur-to-save input for retail and consumable qty
   - Per-item notes textarea (700ms debounced save)
   - Checkbox to confirm packed (card turns green)
   - Walk-in items added via "Add Walk-in Item" dialog (POST /api/zenoti/fulfillments/[id]/items)
   - Stock-on-hand shown per item; shortfalls highlighted amber/red
   - "Download PDF" button available mid-pack (GET /api/zenoti/fulfillments/[id]/packing-slip)
5. Associate clicks "Submit & Email"
   → POST /api/zenoti/fulfillments/[id]/submit
   → Generates PDF (react-pdf) + XLSX (ExcelJS) in parallel
   → Emails both attachments to order@beautylogix.ca (testing; see TYPE_META in lib/zenoti-email.ts)
   → BfsFulfillment status → SUBMITTED
```

### Phase 2 (post-meeting, pending accounting team approval)

```
6. Accounts person reviews email → clicks "Post to QuickBooks" (not yet built)
   → QB Invoice created (warehouse as vendor → service location as QB Customer)
   → BfsFulfillment status → INVOICED, QB invoice number stored
```

### Zenoti API endpoints (assumed — verify with real keys)

| Endpoint                                                                 | Purpose                                                    |
| ------------------------------------------------------------------------ | ---------------------------------------------------------- |
| `GET /v1/centers`                                                        | List all centers for the org (used to discover center IDs) |
| `GET /v1/procurement/purchase_orders?center_id=&statuses=Raised,Updated` | List RAISED+UPDATED POs for one center                     |

No org-wide "all centers" endpoint — BFS fans out one request per `center_id` and merges results.

The actual endpoint path was assumed based on Zenoti API patterns. **Verify once API keys are obtained** — it may differ (e.g. `/v1/inventory/purchase_orders`). The helper is in `lib/zenoti.ts` → `fetchZenotiPOs()`.

### Product matching

Zenoti line items include `product_code` (barcode) and `product_name`. BFS matches by `product.barcode`. Unmatched items are still fulfilable (fulfillment item has `productId: null`, stock-on-hand shows as null rather than crashing).

### Key files

```
lib/zenoti.ts                                             ← API helper (fetchZenotiCenters, fetchZenotiPOs, mapZenotiStatus)
lib/zenoti-excel.ts                                       ← Excel import: parseOrderFile() + upsertZenotiOrder()
lib/zenoti-email.ts                                       ← sendZenotiImportEmail(), getSupplierType(), SupplierType
lib/packing-slip-pdf.tsx                                  ← react-pdf packing slip document + generatePackingSlipPdf()
app/api/zenoti/sync/route.ts                              ← POST — pull RAISED+UPDATED from both orgs (requires API keys)
app/api/zenoti/centers/route.ts                           ← GET — discover center IDs (run once after getting keys)
app/api/zenoti/import-excel/route.ts                      ← POST — multipart upload, parse + upsert one order file
app/api/zenoti/scan-uploads/route.ts                      ← POST — scan uploads/zenoti/{org}/ and import all xlsx files
app/api/zenoti/orders/route.ts                            ← GET — list orders with fulfillment status
app/api/zenoti/orders/[id]/route.ts                       ← GET — order detail + live stock per item
app/api/zenoti/orders/[id]/fulfillment/route.ts           ← POST — create fulfillment from Zenoti order
app/api/zenoti/fulfillments/[id]/route.ts                 ← PATCH — update fulfillment status
app/api/zenoti/fulfillments/[id]/items/route.ts           ← POST — add walk-in item
app/api/zenoti/fulfillments/[id]/items/[itemId]/route.ts  ← PATCH/DELETE — update/remove item
app/api/zenoti/fulfillments/[id]/submit/route.ts          ← POST — email PDF + XLSX packing list, mark SUBMITTED
app/api/zenoti/fulfillments/[id]/packing-slip/route.ts    ← GET — stream PDF download (VIEWER role)
hooks/use-zenoti.ts                                       ← TanStack Query hooks
components/zenoti/dashboard/zenoti-orders-table.tsx       ← orders list with type badges + filter chips
components/zenoti/dashboard/zenoti-page-tabs.tsx          ← tabs: Procurement Orders / Import from Excel
components/zenoti/import/zenoti-import-panel.tsx          ← drag-drop upload + scan-folder UI
components/zenoti/view/fulfillment-view.tsx               ← iPad packing UI (PackingCard, QtyStepperField)
app/(dashboard)/zenoti/page.tsx                           ← thin wrapper
app/(dashboard)/zenoti/[id]/page.tsx                      ← thin wrapper
uploads/zenoti/bfs/                                       ← Excel staging dir (xlsx gitignored, .gitkeep tracked)
uploads/zenoti/bl/                                        ← Excel staging dir (xlsx gitignored, .gitkeep tracked)
public/assets/images/Beauty_Logix_Logo.png                ← logo used in packing slip PDF
```

### DB models added (migration: `20260617033633_add_zenoti_fulfillment`)

```
ZenotiOrder       — cached PO header (zenotiOrderId unique, org "bfs"|"bl", zenotiStatus enum, supplier String?)
ZenotiOrderItem   — line items (productCode=barcode, retailRaised, consumableRaised, unitPrice)
BfsFulfillment    — fulfillment per order (status: PENDING|IN_PROGRESS|SUBMITTED|INVOICED)
BfsFulfillmentItem — packed quantities, isPacked checkbox, isWalkIn flag, notes String?
```

`supplier` field added to `ZenotiOrder` via migration `20260625182009_add_zenoti_order_supplier`.

Enums added: `ZenotiOrderStatus`, `FulfillmentStatus`. `IntegrationProvider` enum extended with `ZENOTI`.

### Getting started after the meeting

1. Get API keys: Zenoti Admin → Setup → Apps → create backend app → copy key
    - `ZENOTI_BFS_API_KEY` from beautyfirstspa.zenoti.com
    - `ZENOTI_BL_API_KEY` from beautylogix.zenoti.com
2. Add both to `.env` and Vercel environment variables
3. Hit `GET /api/zenoti/centers` (authenticated) — returns center IDs for both orgs
4. Test sync: "Sync from Zenoti" button on `/zenoti` dashboard
5. If the sync endpoint path is wrong (lib/zenoti.ts `fetchZenotiPOs`), adjust the path there
6. Once sync works: confirm with accounting team whether QB invoice auto-posting is wanted
7. If yes: build `POST /api/zenoti/fulfillments/[id]/post-to-qb` — creates QB Invoice using existing QB OAuth tokens

### Pricing note

BeautyLogix Zenoti has unit prices on POs; BeautyFirstSpa Zenoti doesn't. QB is the authoritative source for pricing. Phase 1 packing list email contains quantities only (no prices). Phase 2 QB Invoice should pull prices from QB Product `PurchaseCost`.

### Shopify

Shopify is used as a **public-facing storefront** — not the primary revenue channel (locations + Zenoti are). BFS syncs orders for visibility and pushes stock levels so the Shopify store reflects warehouse inventory.

**Two connected stores:**

| Store                      | Domain                    | Auth method                                      |
| -------------------------- | ------------------------- | ------------------------------------------------ |
| Beauty First Spa Wholesale | `ctyvhx-0k.myshopify.com` | OAuth (Partners app, token in DB)                |
| Beauty Logix               | `7f7b80-5d.myshopify.com` | OAuth (same Partners app, different Shopify org) |

**OAuth app**: Created in Shopify Dev Dashboard (dev.shopify.com). Client ID/Secret in `SHOPIFY_CLIENT_ID`/`SHOPIFY_CLIENT_SECRET`. Redirect URI: `https://bfs.kigtech.digital/api/integrations/shopify/callback`. Scopes: `read_orders,write_orders,read_inventory,write_inventory,read_products,read_locations`.

**Connecting a new store**: Integrations → Shopify tab → type `.myshopify.com` domain → Connect. Token stored in `IntegrationConfig.config.stores[]`. For stores in a different Shopify org that block OAuth cross-install, use env var token instead: `SHOPIFY_STORE_N_DOMAIN` + `SHOPIFY_STORE_N_TOKEN` (supports up to 5, merged with DB stores at runtime).

**Sync behaviour:**

- Order sync fetches `status=open` orders. A second pass detects orders that were fulfilled (dropped out of the unfulfilled feed) and fetches each individually to update `fulfillmentStatus` + `shopifyStatus` in BFS.
- Inventory push maps BFS `product.sku` / `product.barcode` → Shopify variant SKU, then calls `/inventory_levels/set.json` at the first active location. 600ms delay between calls (Shopify 2 req/s limit).
- Both sync routes accept optional `{ shop }` body to target a single store (used by per-store buttons in Integrations UI).

---

## Backlog (Prioritised)

### On hold (Zenoti Phase 2 — see note above, likely deprecated)

| Item                            | Notes                                                                                                                                                                                                                                                                 |
| ------------------------------- | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| QB Invoice posting              | Build `POST /api/zenoti/fulfillments/[id]/post-to-qb`. Needs QB customer names per store + accounting team sign-off. Uses existing QB OAuth. Moot if Zenoti is fully retired — Shopify orders already invoice automatically via the QBO Shopify app (see feature 54). |
| Zenoti PO endpoint verification | Confirm actual endpoint path once API keys arrive. Update `lib/zenoti.ts` `fetchZenotiPOs` if needed.                                                                                                                                                                 |

### Medium priority

| Item                        | Notes                                                                                                                                                                  |
| --------------------------- | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| Reorder bulk actions        | Multi-select rows → "Create PO for selected" or "Export selected". Currently only per-row actions.                                                                     |
| Dashboard KPI deltas        | Add ±N trend vs prior week to each KPI card. Data already exists in StockMovement + SalesRecord.                                                                       |
| Analytics date range picker | Analytics charts show fixed period. A date range selector would make it useful for ad-hoc queries.                                                                     |
| Error boundaries            | Pages use `<Suspense>` but no `error.tsx` boundaries. A single component throw crashes the whole page section. `app/(dashboard)/products/error.tsx` exists as example. |

### Skipped (no QB writes policy)

| Item         | Notes                                                                                                                                                |
| ------------ | ---------------------------------------------------------------------------------------------------------------------------------------------------- |
| PO → QB push | Would create QB PurchaseOrder when a BFS PO is sent. Skipped — app is read-only from QB's perspective (except Zenoti invoices which are AR, not AP). |

### Low priority / nice-to-have

| Item                         | Notes                                                                                                                                                                               |
| ---------------------------- | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| Product image gallery        | `Product.imageUrl` field exists, UploadThing wired, upload UI in product form works. Still missing: thumbnail column in products table.                                             |
| PO status timeline           | Show DRAFT → SENT → RECEIVED steps with timestamps on PO detail.                                                                                                                    |
| Email/password auth          | Fallback login if OAuth providers are down. better-auth supports it natively.                                                                                                       |
| Test alert button per rule   | "Send test email" on each alert rule in notifications page.                                                                                                                         |
| AppSetting usage             | Schema has `AppSetting` model but nothing uses it. `SAFETY_DAYS` is now centralised in `lib/sales-calc.ts`; remaining candidates: default location name, email recipients fallback. |
| Observability                | No error tracking. Add Sentry or Axiom for production error monitoring.                                                                                                             |
| Rate limiting on sync routes | QB sync routes can be called without throttle. Could exhaust Intuit API quota.                                                                                                      |

---

## Known Issues / Gotchas

- **axios baseURL is `/api`** — all hook paths must NOT include `/api/` prefix (e.g. `/stock` not `/api/stock`). Double prefix causes 404s.
- **Font variable** — `lib/font.ts` sets `variable: "--font-sans"` directly on `<body>`. Never add an intermediate CSS variable; `:root` (`<html>`) cannot see variables set on its children.
- **Prisma client cache** — after any `prisma generate` / schema change, the dev server must be restarted. New fields return as `undefined` until restart even though the DB column exists. This will cause API routes using new models to throw 500 and return HTML, which axios stores as `data` (not an array) — guard with `Array.isArray()` in hooks if needed.
- **Low stock filter (notification engine)** — uses `prisma.$queryRaw` for DB-level filtering. Do not revert to in-memory — it loads the entire table.
- **Low stock filter (stock page)** — Prisma ORM can't compare two columns. Fetch all, filter in JS. Fine for warehouse scale.
- **PO number generation** — queries last PO `startsWith` current year, increments + zero-pads to 3 digits (`PO-2026-001`).
- **Receive PO** — re-fetches all items after update to recalculate RECEIVED / PARTIALLY_RECEIVED status.
- **Negative QB quantities** — clamped to 0 on import. 96 rows in original spreadsheet were negative.
- **BF Inverness / BLX Inverness locations** — `isActive = false`. Direct-supply locations. Reorder page has "Include direct-supply locations" toggle.
- **Inverness products (81)** — were `isActive = false` (direct-supply, not warehoused). As of the 2026-07 business model shift, BFS now acts as B2B middleman for non-warehoused products too (fulfilled via Shopify), so this segregation no longer applies to `isActive` — the QB active-status sync (see below) will reactivate these on its next run if QBO shows them `Active`. The "not physically in our warehouse" fact itself didn't go away though — it's now tracked separately via `Brand.isWarehoused` (feature 53), which flags Inverness items during Shopify order packing without touching whether the product is active/sellable.
- **Brand lead times** — local brands (Beauty First, Desembre, Fernanda's, Refectocil) = 14 days; all others = 45 days. Set via `scripts/seed-lead-times.ts`.
- **targetStockMonths** — per-product field (not brand). Local brands set to 2 via `scripts/set-local-target-months.ts`. International default = 6.
- **Email notifications** — alerts attach a styled XLSX (ExcelJS) instead of inline product tables. `lib/email-xlsx.ts` → `lib/mailer.ts` (`attachments?: MailAttachment[]`). XLSX is structured as one sheet per brand (alphabetical) + a Summary sheet; both Out-of-Stock and Low-Stock items appear together per brand sheet.
- **TanStack Query v5 `onSuccess`** — inline callbacks need explicit type annotations: `onSuccess: (result: MyType) => { ... }`.
- **chart.tsx** — has `// @ts-nocheck` (recharts type incompatibilities with shadcn-starter copy). Do not remove.
- **`qty < 0` guard in adjust route** — allows correction-to-zero; `qty < 0` not `qty <= 0`.
- **QB sales sync quantity** — `SalesByProductServiceSummary` may omit unit qty columns. The sync upsert only overwrites `SalesRecord.quantity` when QB returns qty > 0; otherwise existing quantity (from Excel bootstrap) is preserved. Check `qtyColumnsDetected` in sync response to confirm whether QB is returning units.
- **QB sales sync removed from cron** — QB OAuth user lacks Reports permission (403). Sales sync can still be triggered manually from the Integrations UI.
- **Cross-route imports in App Router** — never import types or functions from `app/api/.../route.ts` files into other route files. Even `import type` can cause 502s due to bundler behaviour. Put shared helpers in `lib/` instead (e.g. `lib/qbo.ts`, `lib/zenoti.ts`).
- **Shared Neon DB** — local dev and prod point to the same database. Destructive scripts (seed, reimport) run against live data. Always confirm before running import scripts locally.
- **Cron route auth** — `GET /api/cron/sync` and `GET /api/cron/sync-names` both require `Authorization: Bearer <CRON_SECRET>`. Must be set in both `.env` (local) and Vercel environment variables (prod). Vercel native cron sends this header automatically.
- **QB name sync — no-SKU items** — items without a QB `Sku` field are never renamed (counted as `noSku` in response). To rename those products, add SKUs to them in QB first, then re-run the sync.
- **Vendor sync — no overwrite** — existing supplier fields are only filled if currently blank. If a supplier's email was manually edited in BFS, QB vendor data will not overwrite it.
- **Zenoti sync endpoint path** — `lib/zenoti.ts` assumes `/v1/procurement/purchase_orders`. This is unverified — confirm with real API keys and adjust if the actual path differs.
- **Zenoti BfsFulfillmentItem.zenotiItemId** — stores the BFS-internal cuid of the `ZenotiOrderItem` record (not Zenoti's native item ID). Naming is slightly misleading but consistent internally.
- **recharts width(-1) warning** — pre-existing, appears when charts are in hidden/collapsed containers. `// @ts-nocheck` already on chart.tsx; ignore this warning.
- **next/image `sizes` prop** — required for `fill` images. Product view uses `sizes="(max-width: 1024px) 100vw, 33vw"`, product form uses `sizes="(max-width: 640px) 100vw, 512px"`. Always include `sizes` when using `fill`.
- **`NEXT_PUBLIC_APP_URL` must be set in Vercel** — cron routes validate this isn't localhost before making sub-fetches. Set to `https://bfs.kigtech.digital`.
- **`lib/sales-calc.ts` is the single source for avgMonthly** — `computeAvgMonthly()` and `SAFETY_DAYS` are imported by `app/api/reorder/route.ts`, `app/api/inventory/calculate-minimums/route.ts`, and `app/api/settings/stock-policy/route.ts`. Do not redefine SAFETY_DAYS or an inline average calculation in those routes; change it in `lib/sales-calc.ts` only.
- **QB active-status sync is bidirectional and QBO-authoritative** — `resolveProduct()` in the stock sync route no longer filters by `isActive`, so both directions can match against any BFS product regardless of current status: the main loop (active QBO items) reactivates a matched product that was `isActive = false`; the second pass (inactive QBO items) deactivates a matched product that was `isActive = true`. Matching order is SKU → barcode → exact name → hierarchy-stripped name, so items with no QB SKU still match by name (product names are treated as unique for this purpose). Any product manually set inactive in BFS for a reason unrelated to QBO status will be flipped back to active on the next sync if QBO shows it `Active` — there is no override field to prevent this.
- **StockMovement type classification** — `RECONCILIATION` and `OPENING_STOCK` are balance snapshots, not real stock events. Both are excluded from `totalIn` in the movements summary and cards aggregation. Only `PURCHASE_RECEIPT`, `ADJUSTMENT_IN`, `TRANSFER_IN` count as received; only `SALE`, `ADJUSTMENT_OUT`, `TRANSFER_OUT` count as dispatched. Do not add `RECONCILIATION` or `OPENING_STOCK` back to either `IN_TYPES` (movements route) or the summary SQL.
- **QB stock sync delta tracking** — `POST /api/integrations/quickbooks/items` compares `existing.quantity` to `item.QtyOnHand` before upserting. Writes `ADJUSTMENT_OUT` (dispatched) when delta < 0, `ADJUSTMENT_IN` (restocked) when delta > 0, nothing when delta = 0. Only writes `RECONCILIATION` when `existing === null` (first-ever sync for that product). The nightly cron and the manual UI button both use this route.
- **Backfill movements `balanceAfter = 0`** — movements written by the QB invoice backfill route (`POST /api/integrations/quickbooks/backfill-movements`) have `balanceAfter: 0`. Historical balances cannot be reconstructed without full event replay. The "Stock on Hand After" column in the individual movements view will show 0 for these rows — this is expected and noted in the route.
- **QB invoice attribution — multi-store notes** — if the same product is on multiple invoices in one sync cycle, notes reads `"QBO sync: dispatched to Square One, Limeridge Mall"`. The Dispatch by Store report parses this as a single combined key rather than splitting qty between stores. For accurate per-store attribution, the per-invoice movement data (from the backfill, one movement per invoice) is more reliable.
- **Pending product `suggestedBrandName`** — derived by splitting QB `FullyQualifiedName` on `:` (e.g. `"Zensa:Numbing Cream"` → `"Zensa"`). This is a hint only — confirm against real brand list before approving.
- **Dispatch by Store report — `Unknown` store** — movements with `notes = "QBO sync: dispatched"` (pre-attribution or failed invoice fetch) are grouped under `Unknown`. These 30 records are pre-dating the invoice attribution feature (feature 32) and cannot be retroactively attributed.
- **`ProductSupplier.cost` update is `updateMany`** — the cost sync updates ALL `ProductSupplier` records for a product with the same `PurchaseCost` value, regardless of which supplier. QB's `PurchaseCost` is a single global cost; if products have multiple suppliers at different costs, prefer the preferred-supplier record.
- **QB brand/category sync is one-directional and QBO-conditional, not authoritative like active-status** — unlike the bidirectional active-status sync, `ParentRef`/`ClassRef` only ever write BFS → nothing pulls the other way, and a product is only touched when QBO actually returns that field for it (`item.ParentRef?.name` / `item.ClassRef?.name` truthy). Products QBO hasn't classified yet keep whatever brand/category they already have in BFS. `resolveBrandId()`/`resolveCategoryId()` find-or-create by case-insensitive exact name match — a typo'd Class name in QBO will silently create a new duplicate-ish Category row rather than erroring.
- **PendingProduct `seenCount` increments** — the Prisma upsert uses `{ increment: 1 }` on `seenCount` each sync. This only works in Prisma 7 with the raw `increment` syntax on `update`; do not change to a manual `seenCount + 1` read-modify-write.
- **INTERNAL_USE classification** — `INTERNAL_USE` is in `SUBTRACT_TYPES` in the adjust route (reduces stock). It is NOT in `OUT_TYPES` in the movements summary SQL — it is excluded from store dispatch totals and the Dispatch by Store report. Do not add it to either list.
- **QB sync INTERNAL_USE reconciliation** — before writing `ADJUSTMENT_OUT` for a stock drop, the sync sums `INTERNAL_USE` movements for that product since `lastSyncAt` and reduces the unexplained delta by that amount. `lastSyncAt` is hoisted outside the invoice-fetch `try` block so the delta loop can reference it.
- **ProductCombobox requires `useProductsMinimal()`** — do not use `useProducts({ limit: 1000 })` for product pickers. The regular endpoint caps at 100 products per page and has no way to fetch all 901 active products in one call. `?minimal=true` bypasses pagination entirely.
- **ESLint: 86 warnings in codebase** — pre-existing unused imports and `any` types. These are warnings, not errors, and do not block commits. `no-explicit-any` is intentionally set to warn (not error) because API routes use `any` legitimately in catch blocks and external API shapes. Fix as you touch files.
- **`product-view.tsx` has `@ts-nocheck`** — same reason as `chart.tsx` (recharts type incompatibilities). ESLint `ban-ts-comment` is off for both files. Do not remove.
- **pnpm only** — project uses pnpm (pnpm-lock.yaml). Never run `npm install` or `yarn` — it will corrupt the lockfile. Use `pnpm add`, `pnpm install`, `pnpm exec`.
- **`useImportZenotiExcel` must use native `fetch`** — the `api` instance in `lib/axios.ts` sets `Content-Type: application/json` globally, overwriting FormData's `multipart/form-data` boundary. The file upload mutation in `hooks/use-zenoti.ts` uses `fetch('/api/zenoti/import-excel', ...)` directly. Do not change it to `api.post` — the server will reject the upload.
- **`@react-pdf/renderer` and `exceljs` in `serverExternalPackages`** — both are native Node modules that Turbopack cannot bundle. Both must remain in `serverExternalPackages` in `next.config.ts` or API routes using them will fail. `xlsx` is also there for the same reason.
- **React-pdf: no external fonts** — `lib/packing-slip-pdf.tsx` uses built-in `Helvetica` / `Helvetica-Bold`. Do not add Google Fonts CDN URLs — they 404 at PDF generation time. Bold text requires `fontFamily: 'Helvetica-Bold'`, not `fontWeight: 700` (ignored for standard fonts).
- **Zenoti import emails: all to `order@beautylogix.ca`** — `TYPE_META` in `lib/zenoti-email.ts` routes all supplier types to `order@beautylogix.ca` for testing. Before go-live, use the Email Recipients tab in Settings (or update `RECIPIENT_DEFAULTS` in `app/api/settings/email-recipients/route.ts`) to set COSTCO and INVERNESS to `accounting@beautyfirstspa.com`.
- **`getEmailRecipients()` is in `lib/email-recipients.ts`** — do not import the `RECIPIENT_KEYS` / `RECIPIENT_DEFAULTS` from `app/api/settings/email-recipients/route.ts` into other route files. App Router bundles routes independently; cross-route imports cause 502s. The shared helper lives in `lib/`.
- **Submit route no longer sends email** — `POST /api/zenoti/fulfillments/[id]/submit` only marks `SUBMITTED`. Email is a separate explicit action via `POST /fulfillments/[id]/send-email`. Do not add `sendMail` back to the submit route.
- **`POST /orders/[id]/notify` reads recipient from AppSetting** — the notify route uses `getSupplierType()` to determine `SupplierType`, then looks up the matching `settingKey` in `lib/email-recipients.ts`. If AppSetting rows don't exist yet (fresh DB), it falls back to `RECIPIENT_DEFAULTS`.
- **Zenoti Excel order ID format** — `MANUAL-{ORG}-{orderNumber}` (e.g. `MANUAL-BFS-4072`). API-synced orders (once keys arrive) use Zenoti's native IDs. No collision risk, but the two sets are visually distinguishable by this prefix.
- **`BfsFulfillmentItem.notes`** — added when the iPad UI was built but the field must exist in the schema. Confirm via `prisma studio` if you see null errors on the notes save endpoint.
- **QB Sales API sync button removed** — `SalesByProductServiceSummary` returns 403 in production. The "Sync from QuickBooks" card on the Sales tab is gone. Do not add it back. CSV paste (`QbSalesImport`) remains as the manual refresh path.
- **Responsive layout — why `min-w-0` fixes all tables** — `SidebarProvider` renders as `flex` (row). `SidebarInset` is `flex-1` with no `min-w-0`, so its `min-width: auto` lets it expand past the viewport. Adding `min-w-0` to `SidebarInset` (in `app/(dashboard)/layout.tsx`) constrains it, activating the `overflow-x-auto` that `components/ui/table.tsx` already has on its wrapper div. Do not add `overflow-x-auto` to individual table components — the root cause is the flex ancestor, not the tables.
- **Responsive layout — form grids** — all `<Sheet>` forms use `grid grid-cols-1 gap-3 sm:grid-cols-2` for paired fields. Never hard-code `grid-cols-2` without a responsive fallback — on 375px a two-column grid gives ~150px per column which is too narrow for most inputs. Exception: very short numeric pairs (Qty / Unit Price in PO line items) may stay at 2 columns.
- **Responsive layout — breadcrumbs on mobile** — `components/layout/breadcrumbs.tsx` hides non-last crumbs below `sm:` (`hidden sm:flex`) and truncates the current page label at `max-w-[14rem]`. The header left-side div has `min-w-0 flex-1` so breadcrumbs can't push the theme toggle off-screen. Do not remove these constraints.
- **`min-h-dvh` not `h-screen` for full-bleed layouts** — `h-screen` on iOS Safari clips content behind the address bar. `min-h-dvh` (dynamic viewport height) resolves this. Login page uses `min-h-dvh`; any future full-screen layouts should do the same.
- **`export const viewport: Viewport`** — Next.js 15+ App Router requires the viewport meta to be exported separately from `metadata`, not embedded in it. `app/layout.tsx` exports both `metadata` and `viewport` as named exports.
- **Email HTML — always use `<!DOCTYPE html>` wrapper** — bare `<div>` emails without a `<!DOCTYPE html><html lang="en"><head>…</head><body>…</body></html>` wrapper render incorrectly in Outlook and some mobile clients. All email HTML in this project uses the full wrapper pattern. Never reduce a new email template to a bare `<div>`.
- **Email HTML — no emoji as icons** — emoji (`⚠️`, `📦`, `🚨`, `✅`) were removed from all email templates. Use styled text, coloured `<span>` badges, or `<strong>` labels instead. Emoji render inconsistently across email clients and screen readers announce them awkwardly.
- **Email HTML — colour-coded headers per alert type** — `lib/email-templates.ts` `base()` takes a `headerBg` param: red (`#dc2626`) for out-of-stock, amber (`#d97706`) for low-stock, purple (`#7c3aed`) for reorder-needed, green (`#16a34a`) for test. The CTA `.btn` inherits the same colour. Do not revert all headers to black `#111`.
- **Email HTML — table label style** — metadata labels in all email tables use `font-size:12px; text-transform:uppercase; letter-spacing:0.4px; color:#64748b` to visually separate labels from values. New emails should follow this pattern.
- **XLSX freeze rows** — all ExcelJS workbooks should freeze headers. Stock alert XLSX: Summary sheet and brand sheets both have `ws.views = [{ state: 'frozen', xSplit: 0, ySplit: 1 }]`. Packing list XLSX: `ws.views = [{ state: 'frozen', xSplit: 0, ySplit: 5 }]` (rows 1–4 are title/meta, row 5 is the column header).
- **Shopify OAuth — no shop cookie check** — the callback route verifies HMAC only, not a shop cookie. Shopify returns its canonical internal domain in callbacks (`ctyvhx-0k.myshopify.com`) regardless of what the user typed in the connect form. Matching the typed domain against the callback domain would cause false `shop_mismatch` failures; HMAC is sufficient proof of authenticity.
- **Shopify `read_locations` scope required** — `fetchShopifyLocations` calls `/locations.json` which requires `read_locations`. Without it, inventory push returns 403. The scope is declared in the connect route and must also be configured in the Dev Dashboard app version.
- **Shopify stale-order second pass — empty `notIn` guard** — if no unfulfilled orders are returned from Shopify, `seenIds` is empty. Prisma `{ notIn: [] }` can return zero results. The sync route guards with `seenIds.size > 0` before adding the `notIn` filter; omitting it means all unfulfilled BFS orders for that store are treated as potentially stale and individually re-fetched.
- **Shopify fulfilled orders stay in orders table** — `GET /api/shopify/orders` does not filter by `fulfillmentStatus`. All orders are shown with a Fulfillment badge (green = fulfilled, amber = unfulfilled, blue = partial). Use the fulfillment filter dropdown on `/shopify` to narrow. The `shopifyStatus: 'open'` filter is also removed — orders with any status are returned.
- **Shopify `fulfillmentStatus` vs `shopifyStatus`** — an order can be `fulfillmentStatus: 'fulfilled'` but `shopifyStatus: 'open'` if the merchant fulfilled it without archiving. The stale-order pass updates both fields from the individual Shopify order fetch. Archiving in Shopify sets `status: 'closed'`; BFS stores this as `shopifyStatus: 'closed'`.
- **Shopify cross-org install blocked** — if two stores are in different Shopify merchant organizations, the same Partners app cannot be OAuth-installed on both without a public App Store listing. Workaround: create a custom app in the second store's admin (Settings → Apps → Develop apps) to get a `shpat_` token, then use `SHOPIFY_STORE_N_DOMAIN` + `SHOPIFY_STORE_N_TOKEN` env vars. The `getShopifyStores()` env fallback merges with DB OAuth stores at runtime.
- **Shopify inventory push — first active location only** — `setInventoryLevel` pushes to `locations[0].id` (first active location returned by `/locations.json`). If a store has multiple warehouses, only the first is updated. Extend `sync-inventory/route.ts` if multi-location push is ever needed.
- **Shopify Client Secret rotation** — the `SHOPIFY_CLIENT_SECRET` is used only during OAuth token exchange (connect + callback routes). It is NOT the access token. Rotating it invalidates in-flight OAuth flows but does not affect already-connected stores (their `shpat_` access tokens are stored independently in `IntegrationConfig`).
- **Shopify `vendor`/`product_type` are product-level, not variant-level** — unlike price (per variant) and inventory (per variant), Shopify has no per-variant brand/category field. `sync-inventory/route.ts` picks the brand/category from whichever variant SKU on that product first resolves to a classified BFS product and writes it once for the whole product. If a single Shopify product legitimately bundles variants from different BFS brands (not expected in this catalog — it's effectively 1 SKU = 1 simple product), only the first matched variant's classification wins.
- **`vendor`/`product_type` push is one-directional and additive-only, like the QB→BFS brand/category sync** — it never reads from Shopify back into BFS, and a product is only written when BFS has a non-null `brand`/`category` that differs from what Shopify currently has. Unclassified BFS products (brand/category not yet synced from QBO) leave Shopify's existing values alone — this makes repeated Push Stock runs safe as QBO classification data fills in over time.
