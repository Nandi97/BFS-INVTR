import { FormulaView } from '@/components/reorder/formula/formula-view';

export const metadata = { title: 'Reorder Formula — BFS Inventory' };

export default function FormulaPage() {
	return <FormulaView />;
}
